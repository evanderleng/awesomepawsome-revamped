for (var i = 0; i < collections.length; i++) { print('Collection: ' + collections[i]); /* print the name of each collection*/ db.getCollection(collections[i]).find().forEach(printjson); /*and then print the json of each of its elements*/ }
var collections = db.getCollectionNames();
for (var i = 0; i < collections.length; i++) { print('Collection: ' + collections[i]); /* print the name of each collection*/ db.getCollection(collections[i]).find().forEach(printjson); /*and then print the json of each of its elements*/ }
var collections = "orders"
for (var i = 0; i < collections.length; i++) { print('Collection: ' + collections[i]); /* print the name of each collection*/ db.getCollection(collections[i]).find().forEach(printjson); /*and then print the json of each of its elements*/ }
var collections = db.getCollectionNames();
db.CollectionName.find(cart)
db.CollectionName.find(orders)
listDatabases
dbCollection
show orders
show collections
show collecitons
dir
ls
use awesomepawsome
user awesomepawsome
ls
config
for (var i = 0; i < collections.length; i++) { print('Collection: ' + collections[i]); /* print the name of each collection*/ db.getCollection(collections[i]).find().forEach(printjson); /*and then print     the json of each of its elements*/ }
var collections = db.getCollectionNames();
show tables
for (var i = 0; i < collections.length; i++) { print('Collection: ' + collections[i]); /* print the name of each collection*/ db.getCollection(collections[i]).find().forEach(printjson); /*and then print the json of each of its elements*/ }
var collections = db.getCollectionNames();
db.getCollectionNames()
awesomepawsome.colleciotn
awesomepawsome
show collections
db.awesomepawsome(users).find().pretty()
db.awesomepawsome.find().pretty()
db.awesomepawsome.find()
users.find
show orders
show products
show users
show collections
use awesomepawsome
show collections
show awesomepawsome
show databases
show collectoins
listst